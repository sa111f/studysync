# ── Dev profile settings ───────────────────────────────────────────────────────
# These only apply when spring.profiles.active=dev

# Set to true to wipe all users + sessions every time the app starts.
# Useful for a clean slate during rapid UI testing.
# Set to false to keep your test accounts between restarts.
app.dev.reset-on-start=false
